# Second Display 项目协作约定

本文件适用于整个仓库。修改子目录中的代码、配置、资源、测试或发布脚本时都应遵循。

## 开始任务前

1. 阅读 `docs/CGVirtualDisplay_Technical_Design.md` 中与任务对应的章节。
2. 阅读 `docs/AI_CODING_TASKS.md` 中对应任务及其验收标准；没有任务编号时先确认最小改动范围。
3. 阅读相关现有源码和测试，先判断功能是否已经实现或与现有能力重叠。
4. 查看 `git status --short`，保留用户已有改动，不覆盖、不清理无关文件。

## 实现约束

- 不扩大任务范围，不顺带重构无关模块。
- `CGVirtualDisplay` 私有类型只能出现在
  `macos/VirtualDisplayCore/PrivateAPIShim/`；其他目标只能依赖公开包装类型。
- 不删除、跳过或弱化已有测试。行为变化必须补充对应测试。
- 异步流程必须支持取消，并用 generation/session 身份阻止旧回调恢复已结束会话。
- 失败必须返回现有项目错误码；不得使用 `fatalError`、强制解包或泄露敏感信息的日志。
- 保持公开接口和协议向后兼容，除非任务明确要求破坏性修改。
- 签名证书、私钥、Profile、密码、设备日志和构建制品不得提交。

## 验证要求

项目级验证技能位于
`.agents/skills/second-display-validation/SKILL.md`。修改产品代码、构建配置、运行时资源或协议后，
必须读取并执行该技能；不能只依据编译成功宣称完成。

- 所有改动：运行与改动范围对应的自动化检查，并执行 `git diff --check`。
- 任一可执行产品代码或配置改动：至少执行一次 120 秒 HarmonyOS 真机门禁。
- 媒体、输入、网络、连接恢复、生命周期、旋转、后台恢复或性能改动：执行对应交互场景，
  并完成 5 分钟长稳真机门禁。
- 发布候选：执行完整自动化检查、DMG/HAP 构建验证和 5 分钟真机回归。
- 纯文档、注释、网站文案改动可以免真机，但必须明确说明未影响可执行产物。

真机不可用、授权未完成、未安装本次构建或指标未达门槛时，状态只能报告为“未验收”或
“被阻塞”，不能报告任务完成。验证证据保存到 `.build/validation/`，不要加入 Git。

## 完成汇报

汇报必须包含：

1. 实际修改范围；
2. 执行过的完整命令及结果；
3. 真机型号、系统、应用版本、测试时长和场景；
4. 实测 FPS、帧间隔 P95/P99、编码/解码 P95、掉帧率及是否达到门槛；
5. 未执行、被跳过或仍需人工确认的项目；
6. 是否提交、推送或产生发布制品。

只有自动化检查、适用的真机用例和量化指标全部通过后，才可以声明功能验收完成。
